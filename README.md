# AFL Calendar Feeds

This repo generates calendar .ics files for AFL teams from fixturedownload JSON and serves them via GitHub Pages (docs/).

Enable GitHub Pages to serve from the `docs/` folder on the main branch. The Melbourne feed will be at :

https://<username>.github.io/afl-calendar-feeds/melbourne.ics

Workflow runs on a schedule and can be triggered manually.
